'use strict';

{
  const btn = document.getElementById('btn');

  btn.addEventListener('click',() => {
    const results = ['大吉', '吉', '大凶', '中吉'];
    btn.textContent = results[Math.floor(math.random()) * results.length)];

  });
}